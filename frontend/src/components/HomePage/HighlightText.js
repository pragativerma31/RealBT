import React from "react";

const HighlightText = ({text}) => {
    return (
        <span className="text-richblue-500 font-bold">
            {text}
        </span>
    );
}
export default HighlightText;